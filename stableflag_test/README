The remote machine will be executing commands with mmsh as root. If the public rsa key is not in the authorized_keys file in ~root/.ssh on the remote machine, then mmsh will expect the root password to be manually input. This will freeze the stable flag test.
